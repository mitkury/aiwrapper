# Commit

Commit the intended working-tree changes.

Read [Git commits](../rules/how-to-commit.md), confirm the scope, run the
relevant checks, stage only intended files, and create one clear commit.

Push only when the user explicitly asks for it.
